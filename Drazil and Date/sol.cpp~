#include <bits/stdc++.h>

#define maxn 1000007
#define pp push_back
#define bb pop_back

using namespace std;

int main(){
	int a,b,s;
	scanf("%d%d%d",&a,&b,&s);
	if(a+b < s){
		puts((s-(a+b))%2==0?"YES":"NO");
	}else if(a+b > s){
		printf("NO");
	}else{
		printf("YES");
	}
	return 0;
}
