#include <bits/stdc++.h>

#define maxn 1000007
#define pp push_back
#define bb pop_back

using namespace std;

int main(){
	int n;
	scanf("%d",&n);
	pair<int,int> a[n+7];
	for(int i=1;i<=n;i++){
		a[i].first = i;
		scanf("%d",&a[i].second);
	}
	return 0;
}
