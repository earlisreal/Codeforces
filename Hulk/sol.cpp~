#include <bits/stdc++.h>

#define maxn 1000007
#define pp push_back
#define bb pop_back

using namespace std;

int main(){
	char f[][100] = {"I love it", "I hate it"};
	int n;
	scanf("%d",&n);
	for(int i=1;i<=n;i++){
		printf("%s", f[i%2]);
		if(i<n) printf(" that ");
	}

	return 0;
}
