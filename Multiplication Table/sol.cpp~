#include <bits/stdc++.h>

#define maxn 1000007
#define pp push_back
#define bb pop_back

using namespace std;

int main(){
	int n,x;
	scanf("%d%d", &n,&x);
	int ans = 0;
		while(n>1){
			if((x%n)==0){
				if(x/n<=n)
					ans++;
			}
			n--;
		}
	printf("%d\n",ans);
	return 0;
}
