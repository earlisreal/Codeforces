#include <bits/stdc++.h>

#define maxn 1000007
#define pp push_back
#define bb pop_back

using namespace std;

int main(){
	double a=0,b=0;
	for(int i=0;i<3;i++){
		int temp;
		scanf("%d", &temp);
		a+=temp;
	}
	for(int i=0;i<3;i++){
		int temp;
		scanf("%d", &temp);
		b+=temp;
	}
	int n;
	scanf("%d", &n);
	cout << ceil(a/5) << endl;
	printf("%.0f\n", ceil(a/5));
	puts(ceil(a/5)+ceil(b/10)>n ? "NO" : "YES");
	return 0;
}
